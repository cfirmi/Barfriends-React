import styled from 'styled-components';

const BFBusiness = () => (
  <div style={{marginTop: '100px', color: 'white'}}>
    <h1>
      This is the safe page
    </h1>
  </div>
);

export default BFBusiness;